instructions:
  - template: |
      If the task can be completed accurately without additional details, provide the final answer.
      Otherwise, return a JSON object with a single key "question" whose value is your clarifying question.
      
      Note:
      - Additional details will only be provided if they have been explicitly requested.
      - When generating a clarifying question, ignore any other output or formatting instructions and strictly adhere to the JSON format described.
    position: top
  - template: |
      Any question-and-answer pairs that were previously asked for the current active task will be below:
      {faq}
      
      Please use that information as additional context for your response. If you receive a fallback message indicating that no further details are 
      available, complete the task using the information provided without asking further clarifying questions.
    position: top
